/** The iconic number from the flyer. Shown for fidelity; not a clickable dialer. */
export const PHONE_DISPLAY = '647-483-2705';

export const TAX_RATE = 0.13;

/** Stripe's famous test card — the only "valid" number this simulation accepts. */
export const TEST_CARD = '4242 4242 4242 4242';
